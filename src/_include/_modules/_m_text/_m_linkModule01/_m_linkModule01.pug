mixin m-linkModule01(tg)
	-
		var op = {
			url:       tg.url ? tg.url : "",
			blank:     tg.blank ? tg.blank : "",
			className: tg.className ? tg.className : "",
			download: tg.download ? tg.download : "",
		}

	if(op.blank=="_blank")
		a(href= op.url target="_blank" class=op.className download=op.download)
			span
				block
	else
		a(href= op.url class=op.className download=op.download)
			span
				block